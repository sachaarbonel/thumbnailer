# Run with `dart run ffigen --config ffigen.yaml`.
name: ThumbnailerBindings
description: |
  Bindings for `src/thumbnailer.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/thumbnailer_bindings_generated.dart'
headers:
  entry-points:
    - 'src/thumbnailer.h'
  include-directives:
    - 'src/thumbnailer.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
